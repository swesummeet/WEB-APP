# Variabile d'ambiente per il DATABASE Neon.
# Copia la stringa di connessione "pooled" da: Neon Console → tuo progetto → Dashboard → Connection string
# Esempio: postgres://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# NOTA: questa variabile NON deve avere il prefisso VITE_ perché è usata solo
# nelle API serverless (lato server), mai nel browser.
